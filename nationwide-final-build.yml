name: Nationwide Final Build

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  final-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout website
        uses: actions/checkout@v4

      - name: Build partner and lead system
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import html
          import os

          BASE = "https://free-reliable-appliance-pickup.github.io/free-reliable-appliance-pickup-solutions/"
          ROOT = Path(".")

          STYLE = """
          <style>
          *{box-sizing:border-box}
          body{margin:0;font-family:Arial,Helvetica,sans-serif;line-height:1.65;color:#222;background:#f5f7f6}
          header{background:linear-gradient(135deg,#0b5d35,#087443);color:white;padding:45px 20px}
          .wrap,main{max-width:1000px;margin:auto}
          main{background:white;padding:30px 20px}
          h1,h2,h3{line-height:1.25}
          .card{background:#f2f8f4;padding:22px;margin:20px 0;border-radius:10px}
          .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:18px}
          .btn{
