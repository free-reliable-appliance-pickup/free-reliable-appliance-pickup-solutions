name: Strengthen Washington SEO

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  washington-seo:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Strengthen Washington pages
        run: |
          python3 <<'PY'
          from pathlib import Path

          cities = [
              "seattle","tacoma","bellevue","everett","vancouver",
              "kent","renton","federal-way","spokane","auburn",
              "lakewood","puyallup","redmond","kirkland","bothell",
              "lynnwood","shoreline","burien"
          ]

          for city in cities:
              file = Path(f"{city}-appliance-pickup/index.html")

              if not file.exists():
                  print("Missing:", file)
                  continue

              html = file.read_text(
                  encoding="utf-8",
                  errors="ignore"
              )

              name = city.replace("-", " ").title()

              marker = "<!-- WASHINGTON SEO BOOST -->"

              if marker in html:
                  print("Already updated:", name)
                  continue

              section = f"""
          {marker}
          <section>
            <h2>Washer and Dryer Pickup in {name}, Washington</h2>

            <p>
              Free Reliable Appliance Pickup provides qualifying appliance
              pickup in {name} for washers, dryers, refrigerators, stoves,
              ranges, freezers and other major household appliances.
            </p>

            <p>
              Washer and dryer pickup is one of our primary services.
              Working and reusable appliances may be prioritized for reuse.
              Pickup approval depends on appliance condition, local demand,
              accessibility and scheduling.
            </p>

            <p>
              Send clear photos and accurate condition information with your
              request so we can review whether the appliance qualifies for
              free pickup.
            </p>

            <p>
              Washington customers can call
              <a href="tel:+15038684455">(503) 868-4455</a>
              or use the online pickup request form.
            </p>
          </section>
          """

              if "</main>" in html:
                  html = html.replace(
                      "</main>",
                      section + "\n</main>",
                      1
                  )

              elif "</body>" in html:
                  html = html.replace(
                      "</
