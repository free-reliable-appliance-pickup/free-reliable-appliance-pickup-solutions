name: Restore Nationwide Partners Page - Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Workflow disabled
        run: echo "Old restore workflow is disabled. No website files will be changed."
